# 大营销平台

## 新增功能

### 增加十连抽功能

#### 1. 注意点

- 将权重抽奖修改为：今日抽奖次数达到x次后必得，然后进度每日重新计算。所以，十连抽每次都要计算是否达到权重抽奖条件。
- 部分操作可以使用线程池提高速度(执行相同操作的内容)，如执行抽奖函数：`org.example.domain.strategy.service.IRaffleStrategy#performRaffle`; 发送异步中间消息函数：`org.example.domain.award.service.IAwardService#saveUserAwardRecord`
- 部分操作可以通过批处理函数提高速度（写入数据的内容），如 保存订单信息: `org.example.domain.activity.model.aggregate.CreatePartakeOrderAggregate#setUserTenRaffleOrderEntity`

#### 2. 修改用户今日抽奖次数查询方式

原本查询逻辑是查询mysql获取今日抽奖次数，但导致多线程执行时数据不会及时更新；十连抽时今日抽奖次数都是相同的，故需要将今日抽奖次数存储在Redis中，从而及时更新。

需要两步：

- 查询key是否存储在Redis
- 若存在则`value+ 1`，否则新建key-value键值对。

由于需要保证原子性，使用`lua`脚本实现。

```java
# lua脚本
// 获取当前日期
LocalDate today = LocalDate.now();
// 创建缓存键
String cacheKey = Constants.RedisKey.RAFFLE_DAY_COUNT + userId +Constants.UNDERLINE + strategyId +Constants.UNDERLINE+ today;
// Lua 脚本
String luaScript =
    "local key = KEYS[1] " +
    "local current = redis.call('get', key) " +
    "if current == false then " +
    "    redis.call('set', key, 0, 'EX', 86400) " +
    "    current = 0 " +
    "end " +
    "current = tonumber(current) " +
    "current = current + 1 " +
    "redis.call('set', key, current, 'EX', 86400) " +
    "return current";
// 执行 Lua 脚本
return redissonClient.getScript().eval(RScript.Mode.READ_WRITE, luaScript, RScript.ReturnType.INTEGER, keys);
```





## 压测









## 相关问题

### 线程池使用

1. 线程池的创建和过程

- 核心参数：
  - 核心线程数
  - 最大线程数
  - 空闲线程存活时间
  - 存活时间单位
  - 任务队列（有界数组队列、无界链表队列、优先队列等）
  - 线程工厂：用于定义如何创建新的线程（可以设置线程名称等）
  - 拒绝策略（直接丢弃【默认】、调用线程自己调用、删除无法执行的任务、删除任务队列对头任务）

```java
 ThreadPoolExecutor executor = new ThreadPoolExecutor(
            2,                  // 核心线程数
            4,                  // 最大线程数
            60L,                // 空闲线程存活时间
            TimeUnit.SECONDS,   // 时间单位
            new ArrayBlockingQueue<>(2),  // 任务队列
            Executors.defaultThreadFactory(),  // 线程工厂
            new ThreadPoolExecutor.AbortPolicy()  // 拒绝策略
        );
```

- 线程池使用过程

​	**缺图**

2. 线程提交

线程池提供了`execute`和`submit`两种提交任务方法，当使用这两种方法提交任务时，线程池就会根据自身的线程和任务队列情况来执行线程。

- `execute()`方法

任务类型：`execute`方法只接受实现了`Runnable`接口的任务

返回值：`execute`方法只接受实现了`Runnable`接口的任务，**无返回值**。

异常处理：`execute`方法提交的任务异常会传递给线程池、记录到日志中；但调用者无法直接捕获该异常，通过`ThreadPoolExecutor`的`afterExecute`方法来处理异常。

取消任务：`execute`方法没有返回值，调用者无法取消已提交的任务。

- `submit()`方法

任务类型：`submit`方法既接受`Runnable`任务也接受`Callable`任务。

返回值：`submit`方法返回一个`Future`对象。通过`Future`对象可以：

- [x]  获取任务执行结果
- [x] 判断任务是否完成
- [x] 取消任务的执行
- [x] 获取任务执行中抛出的异常

**注意点**：

- [x] `future.get`方法会阻塞线程，直到任务执行完成。
- [x] `Runnable`任务会返回`Future<?>`对象

异常处理：`submit`方法可以通过调用`Future.get`方法捕获异常。

取消任务：`submit`方法可以通过调用`Future.cancel(true)`方法取消正在执行或未执行的任务。

3. `CompletableFuture`异步编程

`CompletableFututure`实现了`Future`接口，并提供了扩展。

- 功能

`CompletableFuture`提供了多种功能关系，如：

- [x] `thenApply/thenApplyAsync`：第一个任务执行完成后，执行第二个回调方法任务，会将该任务的执行结果，作为入参，传递到回调方法中，并且回调方法是有返回值的。
- [x] `thenAcceptBoth`: 会将两个任务的执行结果作为方法入参，传递到指定方法中，且 **`无返回值`**

。。。

- 异步操作

```java
public static CompletableFuture<Void> runAsync(Runnable runnable)
public static CompletableFuture<Void> runAsync(Runnable runnable, Executor executor)
```

主要区别：

- [x] 使用没有指定`Executor`方法时，内部使用`ForkJoinPool.commonPool`作为线程池执行任务。
- [x] 默认情况下，`CompletableFuture`使用**公共**的`ForkJoinPool.commonPool`线程池，这个线程池默认创建的线程数是 CPU 的核数（也可以通过 `JVM option:-Djava.util.concurrent.ForkJoinPool.common.parallelism` 来设置 `ForkJoinPool` 线程池的线程数）。但所有任务都使用公共线程池时，会导致线程池执行较慢；所以，建议不同任务使用不同的自建线程池。

4. `CountDownLatch`等待线程执行完成

`CountDownLatch`允许一个或多个线程等待其他线程完成操作后再继续执行。

工作原理：

- 初始化一个指定计数器`latch`
- 每个线程执行完成后调用`countDown`将计数器减1
- 主线程调用`await`方法阻塞当前线程，直到计算器为0
- 计数器为0后，所有等待`latch`信号的线程都会被唤醒继续执行。